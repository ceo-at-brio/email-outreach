# email-outreach — third-party deps by script
#
# emails.py
pandas>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
dnspython>=2.4.0
# Optional in emails.py (semantic relevance + KeyBERT); large download (~torch + models)
sentence-transformers>=2.2.0
keybert>=0.8.0
#
# generate_emails_gemini.py
google-generativeai>=0.8.0
#
# generate_outreach_emails.py, email_prompts.py, main.py — stdlib only
